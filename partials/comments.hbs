{{!-- Comments --}}
{{#if comments}}
    <div class="comments p-b-0" id="comments">
        <div class="container wrapper flex flex-col flex-cc">
            {{> artalk-comments}}
        </div>
    </div>
{{/if}}
