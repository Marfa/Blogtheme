{{! This is the post loop - each post will be output using this markup }}
{{#foreach posts}}
  <article class="{{post_class}} wow fadeIn" data-wow-duration=".8s">
    <div class="row">

      <div class="column">
        <header class="entry-header">
            {{#if authors}}
                {{#foreach authors}}
                    <p class="entry-author">Author: <a href="{{url}}">{{name}}</a></p>
                {{/foreach}}
              {{/if}}
          <h2 class="entry-title"><a href="{{url}}">{{{title}}}</a></h2>
          <time class="entry-date" datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMMM YYYY"}}</time>
        </header>

        <section class="entry-content">
          <p class="entry-tags">{{tags prefix="Tags: "}}</p>
          <p>{{excerpt characters="400"}}</p>
          <p><a href="{{url}}" class="more-link">Read more</a></p>
        </section>
      </div>

    </div>
  </article>
{{/foreach}}

{{! Previous/next page links - displayed on every page }}
{{pagination}}
