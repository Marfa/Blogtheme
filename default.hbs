<!DOCTYPE html>
<html lang="{{@site.locale}}" data-grid-columns="3">
<head>
    {{!-- Document Settings --}}
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta property="article:author" content="{{name}}">
    <meta property="telegram:channel" content="@themarfa">
    <meta property="tg:site_verification" content="g7j8/rPFXfhyrq5q0QQV7EsYWv4=">

    {{!-- Page Meta --}}
    <title>{{#is "account"}}{{t "Account"}} | {{else}}{{#is "signin"}}{{t "Sign in"}} | {{else}}{{#is "signup"}}{{t
            "Sign up"}} | {{/is}}{{/is}}{{/is}}{{meta_title}}</title>

    {{!-- Mobile Meta --}}
    <meta name="HandheldFriendly" content="True"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    {{!-- PWA --}}
    <link rel="manifest" href="{{asset 'manifest.webmanifest'}}">
    <meta name="theme-color" content="{{@site.accent_color}}">

    {{!-- All-In-One Person icons (before ghost_head so they take priority) --}}
    <link rel="icon" href="{{asset 'favicon.png'}}" type="image/png" sizes="32x32">
    <link rel="icon" href="{{asset 'icon-48x48.png'}}" type="image/png" sizes="48x48">
    <link rel="icon" href="{{asset 'icon-192x192.png'}}" type="image/png" sizes="192x192">
    <link rel="apple-touch-icon" href="{{asset 'icon-192x192.png'}}">

    {{!-- Theme Configuration --}}
    {{> theme-config}}

    {{!-- Preload CSS only — preloading the full JS bundle competes with LCP --}}
    <link rel="preload" href="{{asset 'dist/app.min.css'}}" as="style">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    {{!-- Fewer weights + optional: avoid Manrope late-swap CLS (CrUX desktop CLS culprit) --}}
    <link rel="preload"
          href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700&display=optional"
          as="style"
          onload="this.onload=null;this.rel='stylesheet'">
    <noscript>
        <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700&display=optional"
              rel="stylesheet">
    </noscript>

    <style>
        {{!-- Metric-matched fallback so optional font swap does not shift layout --}}
        @font-face {
          font-family: 'Manrope Fallback';
          src: local('Tahoma'), local('Arial');
          size-adjust: 105%;
          ascent-override: 90%;
          descent-override: 22%;
          line-gap-override: 0%;
        }
        body {
          --font-sans: var(--gh-font-body, 'Manrope'), 'Manrope Fallback', Tahoma, Arial, sans-serif;
        }
        body h1,body h2,body h3,body h4,body h5,body h6 {
          font-family: var(--gh-font-heading, 'Manrope'), 'Manrope Fallback', Tahoma, Arial, sans-serif;
        }
        .adspace { aspect-ratio: 6 / 1; }
        .adspace img { display: block; width: 100%; height: auto; }
    </style>

    {{!-- Styles --}}
    <link rel="stylesheet" href="{{asset 'dist/app.min.css'}}"/>
    <!--cssential-->
    <style></style><!--/cssential-->

    {{!-- Main theme scripts (async, after CSS link so parse is not blocked earlier than needed) --}}
    <script async src="{{asset 'dist/app.min.js'}}"></script>

    {{!-- Tag archives: keep for users, exclude from Google index --}}
    {{#is "tag"}}
        <meta name="robots" content="noindex, follow">
    {{/is}}

    {{!-- Ghost outputs important style and meta data with this tag --}}
    {{ghost_head}}

    {{!-- Global scripts for setting values --}}
    {{> global-scripts}}
</head>
<body class="{{body_class}}{{#is "page"}}{{#unless
        @page.show_title_and_feature_image}} page-no-title{{/unless}}{{/is}}">
{{!-- Include Search View --}}
{{!-- {{> search}} --}}

{{!-- Include Menu --}}
{{> menu}}

{{!-- The blog navigation links --}}
{{^is "signin, signup"}}
    {{> header}}
{{/is}}

<main class="main">
    {{!-- All the main content gets inserted here, index.hbs, post.hbs, etc --}}
    {{{body}}}
</main>

{{!-- The footer at the very bottom --}}
    {{> footer}}

    {{!-- Scroll Top --}}
    <div class="scroll-top">
        <svg class="progress-circle" width="100%" height="100%" viewBox="-1 -1 102 102">
            <path d="M50,1 a49,49 0 0,1 0,98 a49,49 0 0,1 0,-98"/>
        </svg>
        {{> icon name="arrow-up"}}
    </div>

{{!-- Notifications --}}
{{> notifications}}

{{!-- Service Worker for PWA --}}
<script>
    if (config.enable_pwa && 'serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/service-worker.js');
      });
    }
</script>

{{!-- Ghost outputs important scripts and data with this tag --}}
{{ghost_foot}}
</body>
</html>
